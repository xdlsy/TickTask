# Decision Log — 修订日程功能 PRD

## 会话记录

### 2026-06-05 — 初始化

**决策 1：修订范围**
- 决定：修订范围为当前显示的整周（周一至周日）
- 理由：与现有 generate 行为一致，后端 GenerateSchedule 也是按整周处理
- 状态：已记录

**决策 2：修订预览机制**
- 决定：用户选择方案 B — 显示修订预览弹窗，展示变更对比，用户确认后再应用
- 理由：用户明确偏好此方案
- 状态：已记录

**决策 3：输入方式**
- 决定：用户通过文本输入框输入自然语言 prompt
- 理由：用户明确偏好此方式，灵活度最高
- 状态：已记录

**决策 4：校验流程**
- 决定：修订后必须走校验流程，复用 auto-schedule 的 validate_schedule.py
- 理由：用户明确要求
- 状态：已记录

**决策 5：API 设计**
- 决定：两阶段 API — `/revise` 返回预览差异，`/revise/apply` 确认应用
- 理由：支持预览-确认交互模式；修订未确认前不写入数据库
- 状态：已记录

**决策 6：按钮位置**
- 决定：在"生成日程"按钮右侧
- 理由：用户明确要求
- 状态：已记录

**决策 7：两个待定假设**
- Q1（跨周范围）：默认整周，非阻塞，延期到实现阶段确认
- Q2（手动编辑后修订基线）：默认以 DB 为准，非阻塞，延期到实现阶段确认
- 状态：已记录

### 2026-06-05 — 最终化
- PRD 状态设为 final
- 全部 6 项决策已覆盖，2 个非阻塞假设已延期
