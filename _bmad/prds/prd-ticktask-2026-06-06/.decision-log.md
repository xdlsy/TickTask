# Decision Log — AI Agent 可配置能力 PRD

## 会话记录

### 2026-06-06 — 初始化

- PRD 创建，状态 draft
- 工作区：`_bmad/prds/prd-ticktask-2026-06-06/`

### 2026-06-06 — Discovery

**决策 1：配置范围**
- 决定：用户可配置全部习惯偏好 + 完整 Prompt 模板，不做部分开放
- 理由：用户明确要求"全都能改"
- 状态：已记录

**决策 2：后端发布策略**
- 决定：Claude CLI、OpenCode CLI、API 直连三种后端一个版本全做
- 理由：用户明确要求"一个版本全做"
- 状态：已记录

**决策 3：导出格式**
- 决定：JSON 格式导出/导入
- 理由：用户明确要求 JSON
- 状态：已记录

**决策 4：Stakes 定位**
- 决定：个人项目，轻量 PRD（约 2 页）
- 理由：用户确认是个人项目
- 状态：已记录

**决策 5：habit.md 迁移策略**
- 决定：首次进入设置页时，若 DB 无配置但 habit.md 存在，解析回填到 UI；之后 UI 覆盖文件
- 理由：不丢失用户已有手写配置
- 状态：[ASSUMPTION]，非阻塞

**决策 6：Prompt 模板变量机制**
- 决定：使用 `{{tasks}}` `{{habits}}` `{{week_start}}` `{{week_end}}` `{{learning_log}}` 占位符，后端替换
- 理由：简单直观，与现有模板引擎风格一致
- 状态：已记录

**决策 7：后端抽象**
- 决定：三种后端统一为 `AgentRunner` 接口，`ScheduleService` 面向接口编程
- 理由：取代当前硬编码 `claude -p` 调用
- 状态：已记录

**决策 8：API Key 安全**
- 决定：导出 JSON 不含 API Key，导入后需重新填写
- 理由：安全考虑，防止配置文件泄露密钥
- 状态：已记录

**决策 9：现有 AI 功能范围**
- 决定：不改动现有 LLMClient 接口（分类、insights 等），仅改造日程生成/修订路径
- 理由：用户确认无其他 AI 扩展需求
- 状态：已记录

### 2026-06-06 — 最终化

- 用户审批通过，PRD 进入 final
- 7 项决策已确认，2 个假设（Q1/Q2）延期到实现阶段
