TT_SERVER_HOST=0.0.0.0
TT_SERVER_PORT=8080
TT_DATABASE_PATH=./data/ticktask.db
# Note: TT_AI_API_KEY is no longer read by the server. Enter your API key in
# the Settings page; it is stored encrypted in the SQLite DB.
